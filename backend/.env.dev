# Configuração para desenvolvimento local (sem Docker)
PORT=3013

# Postgres local (você deve ter um Postgres rodando localmente)
POSTGRES_HOST=localhost
POSTGRES_USER=docker
POSTGRES_PASSWORD=password
POSTGRES_DB=abp
POSTGRES_PORT=5432